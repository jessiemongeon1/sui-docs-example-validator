{
  "name": "sui-docs-example-validator",
  "version": "1.0.0",
  "description": "CI pipeline that validates all code examples used in Sui documentation",
  "type": "module",
  "scripts": {
    "discover": "tsx scripts/discover.ts",
    "build-matrix": "tsx scripts/build-matrix.ts",
    "validate": "tsx scripts/validate-package.ts",
    "validate-all": "tsx scripts/validate-all.ts",
    "report": "tsx scripts/report.ts"
  },
  "dependencies": {
    "glob": "^11.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
